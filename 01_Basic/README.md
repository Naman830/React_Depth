# 01_Basic — React Fundamentals

The starting chapter. Read the files in number order.

| # | Topic | What you learn |
|---|-------|----------------|
| 01 | [Set up React environment](01_setUp_react_env.md) | Node & npm, why a build tool is needed, Vite vs Parcel vs CRA vs Next.js, creating an app step by step, every file explained, npm scripts, common errors |
| 02 | [JSX rules in depth](02_jsx_rules.md) | What JSX compiles to, one root element & Fragments, closing tags, camelCase attributes, `{}` expressions, capital component names, what renders and what is skipped, keys, 10 common mistakes |
| 03 | [Components and props](03_components_and_props.md) | Why components exist, capitalized functions returning JSX, `export`/`import`, passing every value type as a prop, destructuring & defaults, `children`, sending data back up with callbacks, one-way data flow, props vs state |
| 04 | [Handling events](04_handling_events.md) | `onClick` and friends, pass vs call the handler, the event object, passing arguments with arrow functions, `preventDefault`, bubbling & `stopPropagation`, keyboard events, event delegation, SyntheticEvent |
| 05 | [Conditional rendering and lists](05_conditional_rendering_and_lists.md) | Guard clauses, ternary, `&&` and the `0` trap, `??` vs `\|\|`, `?.`, object lookups, `.map()` and `.filter()`, empty states, and what `key` really does |

## Coming next
- ➡ [Chapter 02_Hooks](../02_Hooks/README.md) — useState, useEffect and friends

⬅ [Back to master index](../README.md)
